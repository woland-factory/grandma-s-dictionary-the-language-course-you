// Placeholder runtime config for local dev and the built preview. In the
// container this file is regenerated from environment variables before nginx
// starts. All channels default off, so the app runs fine with these values.
window.__APP_CONFIG__ = {
  SEED_DEMO: "",
  UMAMI_URL: "",
  UMAMI_WEBSITE_ID: "",
  SENTRY_DSN: "",
};
